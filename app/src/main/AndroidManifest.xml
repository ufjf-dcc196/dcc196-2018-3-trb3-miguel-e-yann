<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.miguelalvim.fichaacademia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tela_Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Criar_Ficha" />
        <activity android:name=".Criar_Atividade" />
        <activity android:name=".Tela_Edit_Ficha" />
        <activity android:name=".Listar_Atividades" />
        <activity android:name=".Adicionar_atividade_ficha"></activity>
    </application>

</manifest>